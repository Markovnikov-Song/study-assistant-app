import 'package:flutter/material.dart';

/// Stub — 不应该被实际使用
Widget buildMindMapView(String html) =>
    const Center(child: Text('不支持的平台'));

Future<void> saveMindMapImage(BuildContext context, dynamic ctrl) async {}
