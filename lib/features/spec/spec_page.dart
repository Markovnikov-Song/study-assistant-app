// lib/features/spec/spec_page.dart（占位，任务 8 会完整实现）
import 'package:flutter/material.dart';

class SpecPage extends StatelessWidget {
  const SpecPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('Spec 规划模式')),
        body: const Center(child: Text('功能开发中')),
      );
}
