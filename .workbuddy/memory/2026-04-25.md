# 2026-04-25 工作日志

## 今日完成

### 安全审计与修复
对学伴 App 进行了全面的安全审计，修复了 7 个安全漏洞：

**严重漏洞 (4项):**
1. CORS 配置不当 (`api/main.py`) - 移除通配符，改用 origin 白名单
2. JWT 密钥硬编码 (`api/config.py`, `backend/backend_config.py`) - 强制要求环境变量，添加长度验证
3. 管理员接口无权限验证 (`api/routers/token_management.py`, `payment.py`) - 添加 `is_admin()` 检查
4. 支付回调无签名验证 (`api/routers/payment.py`) - 添加签名和 IP 验证

**高危漏洞 (3项):**
5. Dio 日志泄露敏感信息 (`lib/core/network/dio_client.dart`) - 替换 PrettyDioLogger
6. 认证消息暴露用户名 (`api/routers/auth.py`, `backend/routers/auth.py`) - 统一错误消息
7. 缺少安全响应头 (`api/main.py`) - 添加 SecurityHeadersMiddleware

**新增安全组件:**
- `api/security.py` - 管理员验证 + 速率限制装饰器
- `api/security_headers.py` - HTTP 安全响应头中间件
- `docs/SECURITY_AUDIT_REPORT.md` - 完整审计报告

## 待完成（用户需手动处理）
1. 数据库升级：添加 users.role 字段
2. 配置环境变量：JWT_SECRET, CORS_ALLOWED_ORIGINS
3. 支付签名验证：接入支付宝/微信 SDK

## 代码状态
- 所有 Python 文件语法检查通过 ✅

---

### MindMap 结构感知升级 v3

## 改动文件
- **重写** `backend/services/structure_extractor.py` — v2: 英文论文+TF-IDF+公式密度
- **重写** `backend/services/mindmap_service.py` — v3: 两阶段生成+后处理校验
- **更新** `backend/prompts/skills/builtin.yaml` — prompt 适配新流程

## 核心改进
1. 英文论文支持：`1. Introduction / 2.1 Related Work` 格式识别
2. TF-IDF 关键句提取替代简单前 N 行摘要
3. 每节公式密度、定义/例题检测、高频关键词信号
4. 章节重要性评分（篇幅+公式密度综合 0~1）
5. 两阶段生成：fast→提取JSON → heavy→生成markmap（fast不可用退回单次）
6. 后处理校验：层级修正、节点截断(>20字)、噪音过滤、重复去重
7. 降级策略：无骨架→均匀采样，fast失败→单次生成

## 测试验证
- 8 项集成测试全部通过：语法检查、import链、中文教材(7标题)、英文论文(11标题)、定义检测、去重/噪音过滤、JSON解析、空输入降级
- 零 lint 错误
- 接口无破坏性变更：调用方(routers/chat.py, study_planner_service.py)无需任何修改

---

### Flutter 代码清理（22:00-22:30）

**范围：** 227 个 Dart 文件全面审查（死代码、硬编码、代码坏味道）

**P0 修复：**
- 6 处空 `catch (_) {}` → 带标签的 debugPrint 日志
- 删除 `_CompanionBubbleState` 空类，统一到 `_CompanionBubbleOverlayState`
- `_linkColors` 重复定义 → 提取顶层 `_kLinkColors` 常量

**P1 死代码清除：**
- 删除 `app_colors.dart`（133 行死代码）
- `app_theme.dart` 824→100 行：删除 lightTheme/darkTheme/textTheme/AppDecorations

**P1 硬编码修复：**
- profile_page 3 处 + token_usage_page 2 处 iconColor → Theme colorScheme 语义色

**统计：净减 ~950 行 | 0 error | 0 warning**

**遗留 P2：**
- token_detail_page.dart 61 处 Slate 色板硬编码待重构
- app_router.dart legacy 路由别名待 @deprecated
- mindmap_painter.dart 深色模式绿色对比度不足
