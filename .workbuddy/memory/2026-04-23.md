# 2026-04-23 工作日志

## UI Emoji 清理（进行中）

完成了所有 Emoji 的扫描和清理工作：

**已修改文件：**
- `lib/features/auth/login_page.dart`：删掉 `📚`，"伴学" 改为纯文字（字号32，字重800，字间距4），品牌感更强
- `lib/widgets/subject_bar.dart`：`📚` → `Icon(Icons.menu_book_rounded)`；菜单里 `📌📤📦🗑` 全删，仅保留纯文字
- `lib/features/subjects/subjects_page.dart`：菜单里 `✏️📌📤📦🗑` 全删，改为纯文字
- `lib/widgets/session_history_sheet.dart`：过滤标签 `💬🔢🗺🤖` 全删
- `lib/features/history/history_page.dart`：过滤标签 `💬🔢🗺🤖` 全删
- `lib/models/chat_message.dart`：session type 显示文字里的 `💬🔢🗺🤖` 全删
- `lib/models/document.dart`：状态文字 `⏳🔄✅❌` 全删
- `lib/components/notebook/widgets/notebook_card.dart`：`📌📓` → `Icon(Icons.push_pin_rounded/menu_book_rounded)`，带主题色
- `lib/components/library/lecture/export_book_dialog.dart`：`🎉` 删除
- `lib/components/library/editable_mindmap_page.dart`：`🎉` 删除

**保留：** `lib/tools/mindmap/mindmap_painter.dart` 中的 `📖`（Canvas 绘制功能性标记，保留）

## 背景图生成（已完成 ✅）

用 SVG 代码生成了 4 张全页背景插画（完全不依赖在线服务）：

**生成文件：**
- `assets/images/backgrounds/bg_chat.svg` — 答疑室，深靛蓝/紫渐变配浮光球
- `assets/images/backgrounds/bg_library.svg` — 图书馆，暖琥珀金色调
- `assets/images/backgrounds/bg_toolkit.svg` — 工具箱，青绿科技感
- `assets/images/backgrounds/bg_profile.svg` — 我的，珠光深蓝紫

**集成方式：**
- `pubspec.yaml` 添加了 `flutter_svg: ^2.0.10` 依赖
- `shell_page.dart` 新增 `_PageBackground` 组件，背景随页面切换自动切换
- 移除了 library_page、toolkit_page、profile_page 里原有的渐变+圆形装饰，避免叠加

**技术细节：**
- SVG 透明度 dark mode: 0.55，light mode: 0.25 + ColorFilter 提亮
- 每个背景有专属光晕色彩主题，与各工具卡片的渐变色呼应

## README 文档完善（已完成 ✅）

分析了项目源码和配置，编写了完整的 README 文档，包含：

**主要内容：**
- 项目概述和徽章
- 完整的功能特性表格（7大核心模块）
- 技术架构图和栈表
- 详细的项目结构树
- 快速开始指南（后端 + Flutter 两部分）
- 使用示例（登录、AI问答、思维导图、日历、错题本）
- 开发指南（代码生成、路由、状态管理、API调用）
- 贡献指南（开发流程、提交规范、代码规范、分支管理）
- 常见问题 FAQ

**关键信息确认：**
- 项目名称：伴学 (study_assistant_app)
- Flutter SDK: ≥ 3.11.4
- 后端框架：FastAPI + Uvicorn
- 状态管理：Riverpod
- 路由：GoRouter
- 核心模块：Chat、Calendar、Library、Toolkit、Profile、Skill Marketplace

**工具箱图标自定义功能（新增）：**
- 日历工具支持 6 种图标可选
- 用户长按卡片可弹出图标选择器
- 图标选择保存到 SharedPreferences
- 使用 Riverpod `StateNotifierProvider` 管理图标状态
- `toolIconsProvider` 和 `ToolIconsNotifier` 封装在 toolkit_page.dart 中

**日历页面修复：**
- 移除了 `defaultBuilder` 中的 `return null` 逻辑（可能导致 overflow 错误）
- 修复了 `_prefetchAdjacentMonths` 中下个月日期计算错误 `focused.month + 1` → `focused.month + 1, 1`

**手机调试 IP：**
- 热点 IP: 192.168.137.1
- API 配置已更新为 `http://192.168.137.1:8000`

**工具箱图标自定义功能（扩展到全部）：**
- 所有 7 个工具都支持长按选择图标
- 每个工具有 6 种图标可选
- `ToolIconsNotifier` 管理图标状态，保存到 SharedPreferences

**背景风格切换功能（新增）：**
- 新增 `background_style_provider.dart` 管理背景风格
- 在「我的」页面添加「背景风格」入口
- 支持 5 种风格：静谧学习（默认）、极简纯色、活力橙黄、清新绿意、午夜深蓝
- 每种风格 4 个页面背景 SVG（chat/library/toolkit/profile）
- 风格选择保存到 SharedPreferences
- `_BackgroundStyleSheet` 和 `_BackgroundStyleItem` 组件实现选择器 UI
